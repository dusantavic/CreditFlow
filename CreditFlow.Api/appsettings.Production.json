// Production overrides only — merged on top of appsettings.json when
// ASPNETCORE_ENVIRONMENT=Production. No connection string here either:
// in production, it's supplied exclusively via the
// ConnectionStrings__DefaultConnection environment variable, set by
// whatever is running the container (Docker Compose .env file locally,
// or the orchestrator/CI-CD pipeline in a real deployment) — never
// committed to a file in this repo.

{
 
}
